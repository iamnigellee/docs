# 基于 OpenClaw 模式的财务 AI 应用：技术实现分析

> 本文档由 4 个 Claude Code Agent 并行调研产出，分别从 OpenClaw 开发实操、系统集成、竞品分析、架构设计四个维度分析该应用如何落地。

---

## 一、Agent Team 分工

| Agent | 角色 | 调研方向 |
|-------|------|----------|
| Agent 1 | 技术调研员 | OpenClaw SDK/API 开发方式、SOUL.md 写法、工具系统、部署 |
| Agent 2 | 集成架构师 | ERP API、银企直连、金税系统、OCR、消息通道 |
| Agent 3 | 产品调研员 | 竞品分析、已有案例、踩坑经验、开源项目 |
| Agent 4 | 系统架构师 | 整体技术架构、数据流、选型、难点、MVP 建议 |

---

## 二、OpenClaw 开发实操（Agent 1 调研结果）

### 2.1 Agent 创建与 SOUL.md

每个 Agent 是独立工作区，位于 `~/.openclaw/agents/<agentId>/`。核心文件：

| 文件 | 作用 |
|------|------|
| `SOUL.md` | 定义"谁"——角色、风格、规则、上下文 |
| `AGENTS.md` | 定义协作关系 |
| `HEARTBEAT.md` | 定义周期性自动任务 |
| `MEMORY.md` | 长期记忆，每次会话加载 |

**SOUL.md 典型结构**（建议控制在 2000 词以内）：
- **Identity**：角色定位（如"你是财务应付专员 Agent"）
- **Style**：语气风格
- **Rules**：行为红线（如"金额计算必须调用计算服务，不得自行计算"）
- **Context**：业务背景、技术栈

### 2.2 工具系统（MCP）

通过 **MCP（Model Context Protocol）** 对接外部工具：

```bash
openclaw mcp install @modelcontext/server-postgres   # 数据库
openclaw mcp install @modelcontext/server-github      # GitHub
```

MCP 暴露三类能力：**Tools**（可执行动作）、**Resources**（可读数据）、**Prompts**（预设模板）。每个 Agent 可通过 allow/deny 列表控制可用工具。

### 2.3 多 Agent 通信与路由

配置在 `~/.openclaw/openclaw.json` 的 **bindings** 数组：

```json
{
  "bindings": [
    { "agentId": "ap_agent", "match": { "channel": "slack", "peer": { "kind": "channel", "id": "C-payables" } } },
    { "agentId": "ar_agent", "match": { "channel": "feishu", "peer": { "kind": "group", "id": "receivables" } } }
  ]
}
```

路由规则：最具体匹配优先。Agent 间通信通过 Gateway 路由或共享通道中转。

### 2.4 状态管理

- **会话**：基于 channel/peer/agent 三维度隔离，自动剪裁（默认 30 天过期、上限 500 条）
- **长期记忆**：`MEMORY.md` + `memory/YYYY-MM-DD.md`（自动加载今天和昨天）
- **检索**：per-agent SQLite + `sqlite-vec` 向量余弦相似度 + FTS5 全文检索

### 2.5 部署

推荐 **Docker Compose**（官方提供 `docker-compose.yml`）。Gateway 是有状态服务（持有 WebSocket 连接），通常单副本 + 持久卷。

---

## 三、集成层调研（Agent 2 调研结果）

### 3.1 ERP API 现状

| ERP | API 能力 | 认证方式 | 接入难度 |
|-----|---------|----------|---------|
| **金蝶云星空** | REST 风格，覆盖凭证/科目/报表，文档规范 | App ID + Secret → Token，OAuth 2.0 | 中等 |
| **用友 YonSuite** | REST + JSON，覆盖总账凭证/科目余额 | YonBIP 开放平台 | 中等偏高 |
| **用友 U8+** | API 弱，多依赖中间件或数据库直连 | — | 高 |

**建议**：优先金蝶云星空，API 成熟度最高。

### 3.2 银企直连

国内**无统一银行 API 标准**（不同于欧洲 PSD2）：

- **招行 CBS**：API 化程度最高，REST API 支持账户查询/流水下载/支付指令，HTTPS 直连 ← **优先选择**
- **工/农/中/建**：多走专线或前置机，接入周期 1-3 个月，需线下签约
- **第三方聚合**：长亮科技、润和软件 ← 降低多银行对接成本

### 3.3 电子发票/金税系统

两条路径：
- **百望云 / 诺诺网 SaaS API**：REST + JSON，覆盖开票/查验/归集，按量计费 ← **推荐，接入快**
- **直连税局**：门槛高，各省不统一，不建议

### 3.4 OCR 票据识别

| 方案 | 适配性 | 部署 | 成本 | 建议场景 |
|------|--------|------|------|---------|
| **PaddleOCR** | 需自训练 | 私有化 | 低（开源） | 有 AI 团队 |
| **百度 AI** | 预置模型，开箱即用 | 云 API | 按量 | 快速验证 |
| **合合信息** | 财务票据专项优化，准确率最高 | 云/私有化 | 高 | 生产环境 |

### 3.5 消息通道

| 平台 | 机器人能力 | 频率限制 |
|------|-----------|---------|
| **飞书** | Webhook + 应用机器人，支持卡片/交互按钮 | 100次/分 |
| **钉钉** | Webhook + 签名验证 | 20条/分 |
| **企业微信** | Webhook + 应用消息 | 20条/分 |

**集成难度排序**：银企直连 > 金税对接 > ERP API > OCR > 消息通道

---

## 四、竞品与案例分析（Agent 3 调研结果）

### 4.1 市场现状

**国内产品**（合思、每刻、分贝通）：OCR 准确率 97.5%-97.8%，核心在票据识别 + 规则引擎，**大模型深度应用仍处于早期**。

**国外产品**：
- **Brex AI**：近 70% 费用全自动处理，每月节省 20.8 万小时
- **Ramp**：自动化审核率 85%，合规准确率 99%
- **Vic.ai**：AP 专项，需 2-6 周训练期

**结论：国外已从"辅助录入"进入"自主决策"阶段，国内存在空白机会。**

### 4.2 多 Agent 在财务场景的实践

- 对抗性多 Agent 架构在财务对账任务上成功率 **92.1%**，远超单 Agent 的 60%
- **尚未发现 OpenClaw 财务场景的公开案例**，这是蓝海
- LangGraph 适合精确状态管理，AutoGen 适合人工审批门控

### 4.3 关键踩坑经验

1. **AI 幻觉**：财务对数字精度零容忍，必须"技术验证 + 人工复核 + 动态纠错"
2. **审计合规**：AI 可能生成逻辑自洽但偏离法条的解释，必须保留完整决策链路
3. **数据一致性**：数据孤岛和系统兼容是最大落地障碍

### 4.4 可参考开源项目

- [FinRobot](https://github.com/AI4Finance-Foundation/FinRobot)：多 Agent 金融分析平台
- [OpenBB](https://github.com/OpenBB-finance/OpenBB)：金融数据基础设施
- [Firefly III](https://github.com/firefly-iii/firefly-iii)：隐私优先的财务管理

---

## 五、技术架构设计（Agent 4 调研结果）

### 5.1 系统分层

```
┌─────────────────────────────────────────────────┐
│  接入层：OpenClaw Gateway                        │
│  飞书/钉钉 Webhook │ REST API │ 认证鉴权 │ 限流  │
├─────────────────────────────────────────────────┤
│  Agent 层：Orchestrator-Worker                   │
│  总控 Agent → 应付/应收/报销/对账/报表/税务 Agent │
├─────────────────────────────────────────────────┤
│  业务逻辑层：确定性服务（Agent 不做计算！）         │
│  发票校验 │ 金额计算(decimal) │ 三单匹配 │ 税额   │
├─────────────────────────────────────────────────┤
│  数据层                                          │
│  PostgreSQL(业务+审计) │ Milvus(向量) │ Redis(缓存)│
├─────────────────────────────────────────────────┤
│  集成层：适配器模式                               │
│  金蝶 API │ 招行 CBS │ 百望云 │ OCR │ 消息队列     │
└─────────────────────────────────────────────────┘
```

### 5.2 数据流示例：供应商发票 → 付款

```
供应商发票(邮件/飞书) → Gateway → 总控 Agent → 应付 Agent
  → OCR 服务提取发票信息
  → 发票校验服务验真(对接金税)
  → 三单匹配服务(采购订单 + 入库单)
  → ERP 适配器生成应付凭证
  → 金额阈值判断 → 飞书推送审批
  → 审批通过 → 银行适配器发起付款
  → 对账 Agent 监听银行回单确认
  → 全链路审计日志(每步写入)
```

### 5.3 技术选型

| 组件 | 选择 | 理由 |
|------|------|------|
| **LLM** | Claude/GPT-4（复杂推理）+ Qwen（简单分类） | 混合部署，降本 + 数据合规 |
| **向量数据库** | Milvus | 开源自托管，政策文档 RAG |
| **工作流引擎** | Temporal | 长事务、自动重试、Saga 补偿、完整执行历史 |
| **消息队列** | RabbitMQ | 轻量可靠，延迟队列处理对账 |
| **监控** | Prometheus + Grafana + 审计看板 | — |

### 5.4 三个关键技术难点

#### 难点 1：计算精确性
> LLM 仅做意图理解和流程调度，**所有金额运算由确定性代码（decimal 库）执行**。LLM 输出结构化指令而非计算结果。

#### 难点 2：可审计决策链路
> 每个 Agent 调用写入**不可变审计日志**（agent_id、决策依据、引用的政策条款、工具调用参数与返回值）。Temporal Workflow 记录完整执行历史。

#### 难点 3：失败与回滚
> Temporal **Saga 模式**补偿事务——付款失败自动触发凭证冲销；Agent 异常时降级为人工处理队列，飞书通知财务人员介入。

---

## 六、MVP 建议

### 先做什么？**应付流程（AP）**

```
供应商发票接收 → OCR 识别 → 发票验真 → 三单匹配 → 生成凭证
```

**理由**：
- 流程标准化程度最高
- 重复量大，自动化 ROI 最明显
- 仅需对接一个 ERP + 金税系统
- Agent 数量控制在 2 个（总控 + 应付）
- **预计 2-3 个月可交付 MVP**

### MVP 技术栈精简

```
OpenClaw Gateway
  + 2 个 Agent（总控 + 应付）
  + 合合信息 OCR（或百度 AI 快速验证）
  + 百望云发票校验 API
  + 金蝶云星空 API
  + 飞书机器人（审批通知）
  + PostgreSQL + Temporal
```

---

## 七、核心结论

1. **国内市场存在空白**：现有产品停留在 OCR + 规则引擎，多 Agent 智能决策是差异化机会
2. **OpenClaw 可行但无先例**：框架能力匹配需求，但需要自己趟路
3. **LLM 不做计算**：这是财务 AI 的铁律，LLM 只做理解和调度
4. **审计可追溯是刚需**：每个 Agent 决策必须留痕，否则过不了审计关
5. **从 AP 切入**：最标准、最高频、最容易证明价值
6. **集成是最大工程量**：银企直连和金税对接占总工作量的 40%+，优先用第三方服务降低复杂度
